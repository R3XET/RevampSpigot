package org.spigotmc;
/*
import eu.revamp.spigot.utils.chat.color.CC;
import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;

public class MobAICommand extends Command {
    public MobAICommand(final String name) {
        super(name);
        this.description = "Enable or disable MobAI";
        this.usageMessage = "/mobai";
        this.setPermission("*");
    }

    @Override
    public boolean execute(CommandSender sender, String currentAlias, String[] args) {
        if (!this.testPermission(sender)) {
            return true;
        }
        if (SpigotConfig.mobAI) {
            sender.sendMessage(CC.RED + "You have disabled MobAI.");
            SpigotConfig.setMobAI(false);
        } else {
            sender.sendMessage(CC.GREEN + "You have enabled MobAI.");
            SpigotConfig.setMobAI(true);
        }
        return true;
    }
}
*/